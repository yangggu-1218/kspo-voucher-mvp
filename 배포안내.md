# 이번 달 이용권 — 배포 안내

## 1. 가장 빠른 방법 (GitHub Pages)

저장소를 이렇게 만들면 됩니다.

```
내-저장소/
├─ index.html          ← work_v3.html 을 이 이름으로
└─ data/
   └─ nation.json      ← 아래 2번에서 만듭니다 (선택)
```

GitHub 저장소 → Settings → Pages → Source 를 `main` 브랜치 루트로 지정하면
`https://아이디.github.io/저장소이름/` 으로 열립니다. 빌드 과정이 없으므로
파일을 올리는 즉시 반영됩니다.

Netlify·Vercel·Cloudflare Pages 도 똑같습니다. 폴더를 통째로 올리면 끝입니다.
앱이 데이터 파일을 **상대경로**(`./data/nation.json`)로 찾기 때문에
어떤 호스팅이든 설정을 바꿀 필요가 없습니다.

## 2. 전국 데이터 파일 만들기 (권장)

파일 없이도 앱은 동작합니다. 공공데이터 오픈API를 직접 불러오거든요.
다만 그 방식은 API를 97번 호출하고 1분쯤 걸립니다. 파일로 만들어 두면
**요청 한 번에 끝나고 API 일일 트래픽도 쓰지 않습니다.**

1. 배포된 사이트를 엽니다
2. 「강좌 찾기」 → **전국 데이터 불러오기** (약 1분)
3. 헤더의 **관리자 로그인** → `admin` / `kspo2026`
4. 다시 「강좌 찾기」 → **파일로 내보내기** → `nation.json` 이 내려받아집니다
5. 저장소의 `data/` 폴더에 올리고 커밋합니다

이제 방문자는 파일에서 바로 읽습니다. 데이터를 갱신하고 싶으면
「새로 받기」 후 다시 내보내면 됩니다. 월 1회 정도면 충분합니다.

### 파일 크기

| | 크기 |
|---|---|
| nation.json 원본 | 약 6.5 MB |
| 전송 시 (gzip 자동 압축) | 약 1.2 MB |

GitHub 파일 1개 제한은 100MB라 여유롭고, GitHub Pages는 `.json` 을
자동으로 gzip 압축해 보냅니다.

## 3. 앱이 데이터를 찾는 순서

```
① 이 브라우저에 저장해 둔 것 (7일 이내)   → 즉시
② data/nation.json                        → 1회 요청
③ 공공데이터 오픈API                       → 97회 요청, 약 1분
④ 전부 실패                                → 내장 표본 153건으로 동작
```

어느 단계에서 실패해도 다음으로 넘어가고, 다 실패해도 앱은 멈추지 않습니다.

## 4. 배포 전에 손봐야 할 것

### 카카오맵 키 (지도를 쓰려면 필수)
`index.html` 16번째 줄의 `appkey=` 를 발급받은 JavaScript 키로 바꾸고,
[카카오 개발자센터](https://developers.kakao.com) → 내 애플리케이션 →
플랫폼 → Web 에 **배포 도메인을 등록**해야 합니다.
등록하지 않으면 지도 자리에 안내 문구만 뜨고 목록은 정상 동작합니다.

### 검색 노출
현재 `<meta name="robots" content="noindex, nofollow">` 가 걸려 있어
검색에 잡히지 않습니다. 검색으로 사람들이 찾아오게 하려면 이 줄을 지우세요.
다만 이 서비스는 「공단과 무관한 비공식 서비스」임을 명시하고 있어서,
검색에 노출되면 공식 서비스로 오인될 소지가 있습니다.

### 로그인은 진짜 인증이 아닙니다
서버가 없는 단일 HTML이라 비밀번호가 코드 안에 있습니다. 소스를 열면 보입니다.
실제 시행 시에는 서버 인증으로 바꿔야 하며, 교체 지점을 코드에
`AUTH-HOOK` 주석으로 표시해 두었습니다.

### 공공데이터 API 키
`API_KEY` 상수도 클라이언트에 노출됩니다. 조회 전용 키지만 일일 트래픽
한도가 있다면 방문자가 늘수록 소진됩니다. **2번의 데이터 파일 방식을 쓰면
이 문제가 사라집니다** — 방문자는 API를 전혀 부르지 않으니까요.

### 셔틀 운영 여부
실측 데이터가 아니라 기능 확인용 시연값입니다. 실제 운영 현황으로 바꾸려면
`hasShuttle()` 함수 한 곳만 고치면 됩니다 (`SHUTTLE-HOOK` 주석).

## 5. 성능 — 무엇이 깨지지 않게 막아뒀는지

64,153건을 그대로 그리면 브라우저가 멈춥니다. 다음을 넣어 두었습니다.

- **목록**: 한 번에 300건만 그리고 「더 보기」로 늘림
  (필터·정렬·판정 계산은 전체를 대상으로 하므로 표시되는 숫자는 정확합니다)
- **지도**: 마커 100개가 넘으면 자동으로 묶음(클러스터) 표시,
  색깔별 마커 이미지 4개만 만들어 돌려씀, 최대 1,500개까지만 표시
- **지역 선택**: 233개를 시도별로 묶어 표시
- **조합 추천**: 후보를 24개로 제한 (원래부터 적용)

## 6. 테스트

```bash
node tests_map.js
node tests_verdict_combo.js
node tests_advance.js
node tests_auth.js
node tests_errorboundary.js
node tests_shuttle_dvoucher.js
node tests_nation.js
node tests_datafile.js
node qa_sweep.js          # 전 라우트 렌더 + 데이터 정합성 검사
```

`qa_sweep.js` 는 9개 화면을 비로그인·진단완료·로그인·관리자 상태로 전부 렌더해
`undefined`/`NaN` 노출, 렌더 예외, 지역코드 불일치, 종목명 불일치를 잡아냅니다.
데이터를 건드린 뒤에는 이것부터 돌려보세요.
